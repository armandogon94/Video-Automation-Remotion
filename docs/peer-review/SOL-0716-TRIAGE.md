# Triage — Sol (GPT-5.6) follow-up review 2026-07-16

**Findings under triage:** `docs/peer-review/GPT56-FINDINGS-2026-07-16.md` (range `c4a37d6..1ad7bc1`)
**Triage rules:** DOGFOOD-PLAYBOOK §11 — every [OVERTURNED] re-verified with implementer-side evidence before acting; Sol is not automatically right.
**Triage date / executor:** 2026-07-16, Fable (this wave: 10 commits on `main`).

## Bottom line

Every OVERTURNED claim that was re-verified **held up** — zero refutations this round.
Sol's line citations contained one slip (the "25-beat regression" is the
`computeBeatTimings` unit test at `renderFromPlan.test.ts:121`, not the block at 695–735),
but the substantive claim behind it (tests asserted only final totals, never interior
boundaries) was true, so it changes nothing.

## Verdict table

| # | Finding (Sol §) | Verdict | Evidence / disposition | Commit |
|---|---|---|---|---|
| 1 | §2.3 Interior concat joins concealed by global `trim=end_frame` | **CONFIRMED-FIXED** | Reproduced independently with the production filter builder: 25 × 1.03 s unique-gray reel → actual starts `0,31,…,155,186,…,744` vs canonical `…,155,185,…,742` (offsets 0→1→2, tail truncated to hide it) — Sol's exact numbers. Fix: per-beat frame quantization (`fps,tpad=clone,trim=end_frame=N` per input) in **both** staging builders; audio cut to the same canonical length (`apad`+`atrim`); global cap demoted to no-op secondary invariant. Post-fix repro: **all 25 offsets = 0**. New real-ffmpeg test walks per-frame luma and asserts every interior join == canonical `editStartFrame` (mixed fps 25/30/60 + mixed aspects). | `0275aed` |
| 2 | §2.5 Overlay scheduling dual authority (caller `props.exitFrame` wins; multi-source path bypasses sanitizer) | **CONFIRMED-FIXED** | Code-confirmed both paths. New `sanitizeSceneOverlays` is the ONE adapter for single- and multi-source rendering: top-level window is sole authority, `exitFrame` derived unconditionally (planner override dropped with warning), scheduling keys stripped, partial windows dropped loudly, windowless demo overlays untouched. Tests updated + added. | `cc1fb58` |
| 3 | §2.5 / §4.4 / §4.5 `YellowGlowWordCallout` hard-cuts at exitFrame | **CONFIRMED-FIXED** | Code-confirmed (`return null` at `effectiveExit`, no fade — deliberate OV1 replication that violates house lifecycle contract §5 / gate G4). Now fades + scale-eases over its final 6 frames, completing exactly at `exitFrame`; doc block records the deliberate deviation from Hormozi's hard cut. Pixel-level boundary-frame render test in both aspects: **QUEUED** (needs the render-integration harness, see #12). | `cc1fb58` |
| 4 | §3.2 Frame-zero "(YHIGH−YLOW)/2.56" luma stat unsound | **CONFIRMED-FIXED** | Reproduced Sol's counterexample: navy + 7.4%-area gold bar → `YLOW=YHIGH=61` (percentile "σ"=0) vs real per-pixel σ **30.9**. Fix: decode frame 0 as raw gray, compute EXACT per-pixel stddev (`computeLumaStats`, pure + tested with the two-level population case). Explicitly a blank-frame heuristic — report and code say passing it is NOT hook evidence; hook judgement stays the human checklist item. End-to-end verified: sparse-hook σ 30.6 not flagged, flat navy σ 0 flagged. | `db4c3cf` |
| 5 | §3.2 / §4.1 A/V gate compared container vs audio duration (false zero) | **CONFIRMED-FIXED** | Reproduced: 1 s-video/2 s-audio file → container 2.0, audio 2.0, old delta 0 "OK" while the video stream was 1.0 s. Fix: gate now compares VIDEO stream vs AUDIO stream; container demoted to informational; missing video stream is its own loud failure. End-to-end verified: the 1 s/2 s file now MISMATCHES (Δ 1.000). | `db4c3cf` |
| 6 | §3.5 / §1 Lint executes but is not a green gate; `prefer-const` in range | **CONFIRMED-FIXED (in-range portion)** | Baseline reproduced: 68 errors / 31 warnings (`--no-cache`), matching Sol. Exactly ONE error was in the review range (`buildEditPlan.ts:58` prefer-const) — fixed. **Pre-existing debt left and logged: 67 errors / 31 warnings** (compositions, pipeline, demo drivers, e.g. `runAvailV2.ts:19` unused var). Lint remains a NON-gate until that debt wave; do not claim green lint in any verdict. | `4d3257a` |
| 7 | §2.1 `languageProbability` discarded; `--transcript` bypasses the quality warning | **CONFIRMED-FIXED** | Code-confirmed both. One `parseTranscriptPayload` now serves live + precomputed paths; `languageProbability` logged and added as a third suspect signal (<0.6); warning names WHICH signal fired. Smoke-verified on the `--transcript` path (all three signals fire). Kept as a WARNING by design: the multi-take report shows every real raw take trips the w/s check, so the **blocking coverage/corrections gate is QUEUED** behind transcript provenance (P1.2). | `a0cc846` |
| 8 | §2.6 Stale "claude/anthropic map to house marks" comment | **CONFIRMED-FIXED** | The contradictory doc block above `BRAND_ASSETS` removed; one coherent SEMANTIC-RULE block remains. The no-logo override itself was CONFIRMED by Sol and is retained. | `35bf9f7` |
| 9 | §2.6 `SentimentKeyword` is 9:16-documented + off-palette for the 16:9 brand chip | **CONFIRMED-QUEUED** | Component doc says 9:16-only; palette allows cyan/red beyond navy/gold. Correct fix is a dual-aspect `BrandNameChip` on the navy/gold contract emitted by the suggester — molecule design work, queued behind this wave. Current chip is legible (Sol confirmed) so no regression while queued. | — |
| 10 | §3.1 `handleWindows` inputs unsafe (arbitrary numbers, 1-frame zero-opacity flash, overlaps double-mount, twin copies) | **CONFIRMED-FIXED (input-safety half)** | All confirmed by code reading (fade math on a 1-frame window → opacity 0). New SHARED `src/compositions/scenes/handleWindows.ts`: int/nonneg zod fragment + `normalizeHandleWindows` (drop invalid loudly, sort, merge overlapping/touching, 8-frame minimum visible duration); both twins now import it; 8 unit tests. **Production wiring QUEUED**: EditPlan has no cadence field, so the owner default (~3 s per ~45 s + final 4 s) lands with the EditPlan v2 contract; scenes keep the persistent chip meanwhile. | `59ff24f` |
| 11 | §3.4 EditPlan v2 not an authoritative contract (dup ids, mirror unenforced, dangling refs, unsafe migration fill) | **CONFIRMED-FIXED (integrity half) / QUEUED (contract half)** | All four holes reproduced with a schema exercise (dup ids parsed; mismatched mirror parsed; dangling ref parsed with `isSourceAwarePlan`=true + `sourceForSegment`=undefined; two-source omitted selection silently filled with `sources[0]`). Fix: `editPlanSchema.superRefine` (unique ids, legacy mirror, resolvable refs), `isSourceAwarePlan` requires resolvable+unique, migration throws on the ambiguous multi-source half-migration. 6 new tests. **QUEUED:** explicit version discriminator, required source/kind on every v2 segment, single- + multi-source renderer convergence through one source/timing authority (P1.1). | `e1e200c` |
| 12 | §2.2 Layout `camSrc` — rendered-pixel/audio proof missing | **CONFIRMED-QUEUED** | Sol accepted the code fix (prop/component level + unit test); the closure condition is a rendered integration artifact in BOTH aspects with audible audio. Needs a real Chrome render harness run — queued as the first item of the Round-3 session (it also serves the YellowGlow boundary-frame test, #3). Multi-source screen-source selection stays an explicit future decision (never silently reuse the camera file). | — |
| 13 | §2.4 `anullsrc` silence durationed from the drift-prone seconds trim | **CONFIRMED-FIXED** | Folded into #1 exactly as Sol's disposition asked: synthetic silence (and voiced audio) now cut to the canonical `nFrames/fps` length. | `0275aed` |
| 14 | §3.3 Hard gates: G1 not hard, G2/G4 rest on broken evidence, no three-state model | **CONFIRMED-FIXED (docs/process) / QUEUED (tooling)** | Playbook §4.1 now records every gate as **PASS \| FAIL \| PENDING** (pending suppresses scoring like a failure; PASS needs artifact + reviewer), G2 stream-vs-stream, G3 join-caption + baked-text collisions, G4 exact boundary pairs + σ demoted to blank-frame diagnostic. selfEval report states unchecked = PENDING. **QUEUED:** machine-readable gate-state artifact + the blocking transcript coverage gate. | `e47368d`, `db4c3cf` |
| 15 | §4.2–4.6 Round-2 PASS verdicts + weighted scores invalid; REVIEW-R2.html misdescribes the 99 exit | **CONFIRMED-FIXED (record)** | Independently re-verified the decisive G3 claim: transcribed the rendered berman-end1 join snippet myself with medium — audio carries "it is. **They** will help you" while the caption plan jumps `is.` → `will` (plan words verified at frames 406→419 across the seg-0→seg-1 join at f412). ROUNDS.md addendum (append-only) reclassifies all four to FAIL/PENDING and voids the weighted scores; Round 3 required under corrected gates. REVIEW-R2.html (gitignored disk artifact) got a correction banner + the "sale con fade" claim fixed in place. | `e47368d` |
| 16 | §4.2/§4.3 "They"-class caption omission at silence-trim joins | **CONFIRMED-QUEUED** | Root cause: `shiftWordsToEditTimeline` keeps a word iff its START lies in a kept segment; a word starting a hair inside the trimmed gap is dropped from captions while its audio survives the cut pad. A caption-side patch would just move the bleed; the real fix is word-onset snap/padding at cut planning (FABLE Task 2.1 + multi-take report's ~0.1 s pre-voice context) — P0-adjacent, first engineering item after this wave. | — |
| 17 | §5 Multi-take experiment: valuable but not the P1 loop; amended P0/P1 ordering | **CONFIRMED (planning)** | Sol's amended queue adopted as the standing order (below). No code action this wave. | — |

## Standing queue after this wave (Sol §5.2, amended by what landed today)

P0 remainder (before scoring more dogfood):
1. ~~Per-beat frame staging + interior-boundary tests~~ **DONE** (`0275aed`).
2. ~~Single-authority overlay lifecycle incl. YellowGlow + multi-source path~~ **DONE** (`cc1fb58`); handleWindows production wiring rides with EditPlan v2.
3. ~~Repair self-eval evidence + three-state gates~~ **DONE** (`db4c3cf`, `e47368d`); machine-readable gate artifact still open.
4. Word-onset snap/padding for the "They"-class join omissions (#16) + transcript coverage gate (#7) — **OPEN**.
5. Rendered layout-mode integration artifact, both aspects (#12) + YellowGlow boundary-frame render check (#3) — **OPEN** (Round-3 session, needs Chrome).

P1 (unchanged from Sol §5.2): EditPlan v2 authoritative contract + renderer convergence → burst-aware ingest/cache + transcript provenance → automated sequence-aware variant graph on the frozen Claude Cowork fixture → decision-capable review page exporting `takes-decisions.json` → assembly vs published ground truth → semantic decoration planner → corrected hard-gate dogfood round with weighted scoring only after all gates PASS.

## Gates state at end of wave

| gate | state |
|---|---|
| `npx tsc --noEmit` | green |
| `npx vitest run` | **115/115** (was 97 — +18 net: interior-join integration, adapter authority, real-stddev/stream-gate, handleWindows, editplan integrity; −4 removed with the unsound percentile machinery) |
| `uv run pytest` | 13/13 |
| `npm run lint -- --no-cache` | 67 errors / 31 warnings — ALL pre-existing debt outside the review range (was 68/31; the one in-range error fixed). NOT a green gate; logged, not hidden. |
