# Vote 3 — `@builtbystephan` reel `DYkyJfxx5Lx` — seconds 20–28

Independent voting-style analysis, fresh eyes. Reviewed 12 representative frames out of 24 across the seg3 dense-frames window.

Frames reviewed: `seg3-t20.00`, `t20.70`, `t21.04`, `t21.39`, `t21.74`, `t22.09`, `t22.43`, `t22.78`, `t23.48`, `t24.17`, `t24.87`, `t25.57`, `t26.26`, `t26.61`, `t26.96`, `t27.30`, `t27.65`, `t28.00`. (Loaded more than 10 — kept the dense ones around transition points.)

Confidence legend: **[H]** high (visible in multiple frames, unambiguous), **[M]** medium (visible but one frame or ambiguous), **[L]** low (inference / single weak signal).

---

## 1. Overall layout / canvas

- **Split-screen, vertical stack, two equal-ish halves** — top half = screen-recording of a desktop app (Claude / Claude Code style chat UI), bottom half = creator selfie talking-head shot in a brightly-lit home office. **[H]** Present from `t20.00` through `t26.26`.
- **Split ratio shifts during the window** — at `t20.00` the top half holds the screencap and the bottom holds the talking head at roughly a 45/55 split (more face than screen). By `t22.78`–`t26.26` the proportions flip; the screencap card grows and the talking head shrinks to roughly the bottom 35%. **[H]**
- **Layout collapses near the end of the window** — at `t26.96` onward (through `t27.30`, `t27.65`, `t28.00`) the top screencap is **removed entirely** and the creator goes full-bleed full-frame talking head. **[H]** This is the CTA pivot — happens right as the on-screen caption switches to "comment 'brand'" / "and I'll" / "send you".
- **9:16 vertical reel** aspect ratio throughout. **[H]**

## 2. Top-half screen-recording specifics

- **Claude / Claude Code desktop UI being demoed.** Identifiable elements: a dark chat surface with model picker reading **"Sonnet 4.6"** in the lower-right of the composer, a "Write a message…" placeholder, an orange/coral up-arrow send button (rounded square), a `+` attach button on the left, and a microphone icon. Header sidebar/tab title visible: **"Building a prompt assistance website"** with a down-chevron. **[H]**
- **The signature orange asterisk/starburst Claude glyph** appears multiple times — once as a header decoration next to the headline **"Golden hour thinking"** at `t20.70`–`t22.43`, once as a "thinking/working" loader spinner (animated red/orange splat) at `t22.78`, and once as a small mark near the body text at `t24.87`. **[H]** This is the round-ish/asterisk-ish branded element — not a "rotating chrome logo" but it visually pulses/rotates as a working indicator.
- **A small green report-card "🖊 Brand Alchemy Report" header glyph** appears at `t25.57`–`t26.61` next to the heading. The little crayon emoji is the visual anchor. **[M]**
- **Slash command typing animation** — the text `/brand-alchemy I want to build a website that helps people prompt` is **typed in live**, character by character, across `t20.70`→`t22.43`. You can watch it grow: `t20.70` shows just `/brand-alchemy` with a cursor, `t21.39` shows `/brand-alchemy I want to b`, `t21.74` shows `/brand-alchemy I want to build a webs`, `t22.09` shows `…helps peopl`, `t22.43` shows `…helps people prompt`. **[H]** Strong "live typing" effect.
- **Cursor / text-caret animation** — an oversized animated I-beam mouse cursor sits inside the input box during typing, and reappears later in the message area at `t24.87`. The cursor itself is rendered larger than the native macOS cursor — looks like a **CleanShot X / Screen Studio cursor highlight** treatment. **[H]**
- **Skill-execution log lines appear in dark-mode markdown style** — visible text-reveal sequence: "Working", "Reading brand-alchemy skill" (`t22.78`), "Viewed 3 files", "Good, I have the full framework. Now let me run the interrogation phase as the skill requires." (`t23.48`), "Now let me build the names.", "Reading the domain checker script", "Now generating 20+ names using phonosemantic engineering…", "Ran 3 commands", "Good data. Now I have enough to deliver the full brand package." (`t25.57`). These lines appear sequentially — a streamed/staggered reveal that matches Claude's actual output rhythm. **[H]**
- **Markdown-styled inline tokens** inside the chat output: `Read` is shown as a pink/coral-tinted inline code chip; file paths like `references/core-brand-strategy.md` and `references/lexicon-naming-science.md` render as code chips. **[H]** (Visible `t20.70`.)
- **Final report card** at `t26.26`–`t26.61`: heading **"🖊 Brand Alchemy Report"**, subtitle **"The Prompt Engineer Sitting Next to You"**, h2 **"The Top 5 Candidates"**, then `1. 🏆 VIXPROMPT`, sub-paragraph "Fricative + Plosive fusion. The sharpest name in the set.", "Phonosemantics:" block, **"Diamond Test:"** checklist with **green ✅ emoji checkmarks** on Distinctiveness / Fluency / Suggestive / Energy, and a **Domains table** with column headers `Domain | Status` showing `vixprompt.com` → green-checked `AVAILABLE`. **[H]**
- **Background behind the screen-recording card** — a faint blue-toned macOS-style wallpaper edge is visible peeking around the chat window (`t21.04`, `t22.78` show blue strips on the left). The chat card itself is "floating" — it has a soft drop-shadow / rounded corners and sits centered over the wallpaper rather than being a fullscreen-app capture. **[M]** Reads like Screen Studio's automatic window-zoom + background-blur treatment.

## 3. Bottom-half talking-head specifics

- **Same creator throughout** — young man, dark curly hair, dark crewneck sweatshirt, lavalier mic clipped to collar, sitting in front of a white-painted wall with a floating wood shelf (lamp + two small potted plants visible on shelf), a landscape painting (looks like a lake/river scene) on the right wall, and a tall green dracaena plant in the lower left. **[H]** Set is consistent across the entire 8 seconds.
- **Tight chest-up framing** during the split layout — face takes up roughly 50–60% of the bottom half. **[H]**
- **Subtle "punch-in" zoom oscillation** — comparing `t20.00`, `t21.04`, and `t22.09` the camera frame seems to creep in/out very slightly with each new caption word; could be a gentle animated scale on the bottom half or just my eye picking up handheld micro-movement. **[L]**
- **At `t27.30`–`t28.00`** the talking head goes full-frame and you see more of the shoulders/torso plus the chair back (dark grey/black office chair). This is the same shot just un-cropped. **[H]**
- **Creator gesture pattern** — at `t26.96` onward he's holding the lavalier mic up to his chest with his right hand, looking straight to camera. Earlier frames he's mostly still, eye-contact, occasional small mouth movements. **[H]**

## 4. Animated captions (karaoke / TikTok-style)

- **Single-word or short-phrase pop-up captions** centered horizontally, positioned at the seam between top and bottom halves (so they straddle both panels). Examples in order: "called" (`t20.00`), "Brand Alchemy" (`t20.70`), "drop your" (`t21.04`, `t21.39`), "startup idea" (`t21.74`), "it names" (`t22.09`), "your business" (`t22.43`, `t22.78`), "billion dollar" (`t23.48`), "company" (`t24.17`), "the domain's" (`t24.87`), "available" (`t25.57`), "fall in" (`t26.26`), "love" (`t26.61`), "comment 'brand'" (`t26.96`, `t27.30`), "and I'll" (`t27.65`), "send you" (`t28.00`). **[H]**
- **Caption style:** **bold white sans-serif** (looks like Inter / SF Pro / Montserrat Black), uppercase OR title-case (varies — "Brand Alchemy" is title-case while most others are lowercase), **thick black stroke/outline** around each glyph for legibility, no background pill — just stroked text floating on whatever's behind it. **[H]**
- **Pop-in animation** — each caption word appears to swap in/out atomically with each spoken syllable; no slide, no scale-bounce visible across the sampled frames, but the swap rate (one new word every ~0.3–0.5s) suggests a clean text-replacement timed to the voice track. Could be a subtle scale-pop I'm missing between frames. **[M]**
- **Position is stable** — the caption sits at roughly the visual midline (the split seam) for all frames where the split layout is active. When the layout collapses to full-bleed talking head at `t26.96`, the caption migrates to sit roughly over the creator's chest/sweatshirt logo area (still centered horizontally, around y=55–60%). **[H]**
- **Quote marks rendered correctly** — "comment 'brand'" uses real curly double-quotes around the word `brand`. **[M]**

## 5. Colors / palette

- **Top half:** near-black `#101010`-ish chat surface, off-white body text, accent colors are the coral-orange Claude asterisk (`#E07a4e`-ish), pink/coral inline code-chip background for `Read` token, green ✅ emoji checkmarks, soft blue desktop-wallpaper bleed on the edges. **[H]**
- **Bottom half:** warm-neutral home interior — cream/white walls, oak-blonde floating shelf, dark navy sweatshirt, mid-tone skin, dark grey-black chair. Painting on wall introduces sky-blue + green-foliage accents. **[H]**
- **Captions:** pure white fill (`#FFFFFF`) + pure black stroke (`#000000`). **[H]**
- **No teal/gold/pastel branding palette** — the look is "default Claude UI + neutral home + white captions" rather than a custom brand kit. **[M]**

## 6. Logos / chrome / watermarks

- **No personal watermark visible** in the sampled frames — no "@builtbystephan" handle overlay, no logo bug in any corner. **[H]** (Conspicuous absence.)
- **Claude orange-asterisk glyph** is the only logo-like element, and it's a property of the screen recording, not an overlay added by the creator. **[H]**
- **No platform/app logo chrome** like a TikTok watermark, IG Reels chrome — these were stripped (this is a raw mp4, not a re-shared download). **[H]**

## 7. Transitions / cuts

- **Live typing as transition** (`t20.70`–`t22.43`) — instead of a hard cut between two screen states, the typing animation is itself the visual progression. Roughly 1.7 seconds of progressive text reveal. **[H]**
- **Skill-execution streaming reveal** (`t22.78`–`t25.57`) — instead of cuts, each new log line ("Working" → "Reading brand-alchemy skill" → "Viewed 3 files" → narrative response → "Linguistic Brief" → "Brand Alchemy Report") appears as a streamed text-add. This is a very natural "watch Claude work" effect, likely sped-up real footage rather than a faked sequence. **[H]**
- **Top-half scroll/zoom** — between `t23.48` and `t24.17` the chat window appears to scroll down (or the recorded camera pans) so that newer content comes into view. By `t24.87` we see a different region of the conversation. **[H]**
- **Hard cut from split-layout to full-bleed talking head** happens between `t26.61` and `t26.96`. The top half disappears in a single frame. **[H]** No fade, no slide — clean cut. The motivation is the CTA copy change ("fall in love" → "comment 'brand'").
- **No whip-pan, no zoom-blur transitions, no glitch/RGB-split, no rotation effects** visible in this window. **[H]** The video relies on text-reveal pacing rather than transition VFX.

## 8. Shot types

- **Two shot types in play simultaneously** within the split: (a) **screen-recording / over-the-shoulder app demo** in the top half, (b) **chest-up talking-head selfie** in the bottom half. **[H]**
- **Single shot type at the end** (`t26.96`+): **medium close-up talking-head**, slightly wider than the split-mode version, centered. **[H]**

## 9. Audio / VO inference (from caption text)

The 8-second spoken script, reconstructed from caption order:

> "…called Brand Alchemy. Drop your startup idea — it names your business, [the] billion-dollar company, the domain's available, fall in love. Comment 'brand' and I'll send you…"

This puts seconds 20–28 squarely in the **demo → CTA pivot** of the reel. The first ~6s show the skill executing live; the last ~2s collapse to face-cam for the engagement-bait ask. **[H]**

## 10. Notable patterns worth replicating in our 15-template typology

- **Split-screen demo (app on top, face on bottom) with caption straddling the seam** — this is a high-density format that lets the viewer simultaneously watch what's being demoed AND read the creator's emotional reaction. Very efficient for "I built a tool" content. **[H]**
- **Real, sped-up screen recording of an AI skill running** with all the natural beats (typing prompt → "Working" → "Reading skill" → narrative thinking → final structured report). This is more credible than a fake-mockup demo because viewers can see authentic Claude UI chrome (Sonnet 4.6 picker, asterisk loader, code chips). **[H]**
- **Live typing of slash-command prompts** as the opening beat of the demo — clean visual hook, telegraphs "this is a skill / custom command, not just a chat". **[H]**
- **CTA-pivot via layout collapse** — when the script switches from informative to "comment 'brand'", the layout itself changes (split → full-bleed). The form-change reinforces the function-change. **[H]** This is a non-obvious choice worth copying.
- **Static caption position (midline) during split, migrating position when layout changes** — small detail but it keeps captions readable in both modes. **[M]**
- **Pure white text + black stroke captions** — no fancy gradient/highlight word, no animated color sweeps. The discipline of "just legible, just timed" frees attention for the demo itself. **[M]**

## 11. Notable absences (things this creator deliberately doesn't do)

- **No rotating chrome / spinning logos / metallic 3D objects.** **[H]**
- **No b-roll cutaways, no stock footage, no motion graphics overlay.** **[H]**
- **No background music ducking visible from frames (can't confirm audio).** **[L]**
- **No personal watermark / handle overlay anywhere.** **[H]**
- **No emoji-spam in captions** — only the screen-recorded report uses emojis (🏆 ✅ 🖊), and those are part of Claude's output, not added by the editor. **[H]**
- **No progress bar / chapter markers / "1/3" pagination chrome.** **[H]**
- **No zoom-in dolly punches between caption swaps.** **[M]**

---

## TL;DR fingerprint

For seconds 20–28, the editing recipe is: **split-screen vertical (Claude-Code demo on top, talking-head face on bottom) + live-typing slash-command + streaming AI execution reveal + midline-straddling karaoke caption (white-on-black-stroke, one word at a time) → hard cut to full-bleed talking head when the CTA copy lands**. Zero VFX flair, zero transitions beyond a single hard cut, zero watermarks. The "wow" comes entirely from a real-time screen recording of a custom Claude skill producing a polished branded report, paired with the creator's calm chest-up presence underneath.
