# @builtbystephan — Reel `DYkyJfxx5Lx` — Independent Vote 1 of 5 — 0:00–0:10

Voter analyzed 17 of 28 dense frames (seg1) without reading prior analyses or sibling votes. Confidence is marked per observation: HIGH = clear in 3+ frames; MEDIUM = clear in 1–2 frames; LOW = inferred from context.

---

## Frame-by-frame timeline (0–10s)

**t=0.00s** — Split-screen composition is already locked in. TOP HALF: zoomed-in B-roll of a hand holding a BlackBerry phone, the photo placed inside a large mustard/olive ochre filled circle (~#A99845) over a textured cream-paper background with dark speckles. BOTTOM HALF: presenter face-cam — young man, dark curly hair, black sweatshirt with lavalier mic, sitting in a warmly-lit room (white wall, floating shelf with lamp/plants, framed lake painting at right, plant at lower-left). A serif drop-cap word `Blackberry` is overlaid on the upper image (capital `B` is huge and italic). A second caption `The letter` (white, sans-serif, soft drop shadow, centered on the seam between top and bottom halves) sits at the horizontal split line. **HIGH** confidence: split-screen B-roll/face-cam layout is the structural backbone of this first beat.

**t=1.03s** — Same split-screen, same B-roll image, same drop-cap `Blackberry`. The bottom-of-frame caption has advanced to the single word `Blackberry` (the second occurrence — large sans-serif white with subtle drop shadow). Presenter has blinked / micro-moved. Confirms the caption is a **per-word karaoke-style reveal**, not a full sentence.

**t=1.38s** — Same composition, drop-cap `Blackberry` overlay still pinned to B-roll, caption now reads `wasn't random`. Presenter has subtly shifted head pose. The serif drop-cap word is held across multiple captions — it's the topic-label, not a per-word caption.

**t=1.72s** — **HARD CUT** to a full-frame solid blue gradient background (~#1F3A7A → #2C4C9A radial highlight upper-left). Faint thin white **concentric circular arcs** sweep through the lower half — the beginning of a "logo orbit" stage. Only the caption `the company` is on-screen (centered low). The B-roll/face-cam split is gone — we are now in a graphical "logo carousel" stage. **HIGH** confidence on the cut here (zero blend frames between t=1.38 and t=1.72).

**t=2.07s** — Blue stage. A white rounded-square app-icon tile bearing the `pentium PROCESSOR` logo has appeared in the center of the screen. Caption below has rolled to `that named`. Faint concentric arcs still visible. The icon is positioned at the central node of an invisible arc.

**t=2.41s** — Same blue stage. The **`pentium` tile has slid leftward** and is replaced near center-left by the Swiffer tile (chartreuse-green rounded square with the `Swiffer` wordmark). At the far left of the frame a partial Blackberry tile (white square with the `B` mark) is **rotating/tilting off-screen**. Caption: `and`. This is the user-spotted **circular/conveyor-style logo arrangement**: tiles travel along a shared arc, leaning slightly as if mounted on a rotating wheel, entering from one side and exiting the other.

**t=2.76s** — Blue stage. The Swiffer tile has slid to center-left, the `Pentium` text caption appears as a label below. The arc-conveyor continues. **HIGH** confidence: rotating-icon carousel pattern is real.

**t=3.10s** — Swiffer tile is centered, label below reads `Swiffer`. Same arc-orbit composition. Faint concentric arcs are now slightly more visible — they're literally guide-rails the icons travel along.

**t=3.45s** — Swiffer tile begins sliding leftward; a Blackberry icon (white square, BB logo) is now near the center on the conveyor. Caption: `Blackberry`. **HIGH** confidence the orbit is on a left-to-right (or actually right-to-left as we read it) procession.

**t=3.79s** — **HARD CUT** to a brand-new scene: vintage film-stock styled background (Kodak Portra-grade cream/grey vertical strip with `UNFOLD 2010TX` sprocket-numbering printed sideways down both left and right edges, plus tick marks `20`, `21`, `11`, `12`). Centered: a **dark grey rectangular card framed by thin black L-bracket corner marks**. Inside the card: a layered typographic stack — large white serif `Spent` at top, large translucent italic `40` faded mid-card. **HIGH** confidence on the cut (background and color world swap entirely).

**t=4.14s** — Same Unfold-film background. A new sweeping **black brushstroke/ink-swoosh** has wiped diagonally across the dark card. The text stack is now layered with multiple lines visible simultaneously through different opacity: `Spent` (top, opaque white), `40 years` (large translucent outline), `To see how` (left, italic). The card frame brackets and film-strip chrome remain. Looks like a **typographic stack reveal** where successive lines fade in over the previous (not karaoke per-word).

**t=4.48s** — **HARD CUT** to a new scene: pure black background, vertical Unfold film-strip chrome still framing left and right edges (with `21` and `11` tick marks, `UNFOLD 2010TX` text). Center: a 3D-rendered **paper-cutout silhouette of a man with arms outstretched** standing atop a tall white rectangular plinth/pedestal. No caption yet — graphic-only beat.

**t=4.83s** — Same black/pedestal scene. Caption begins: `Make people` in thin grey/white sans-serif at top, animating in word-by-word. The pedestal man is now slightly **rotating** (different angle, longer cast shadow on the pedestal top) — the 3D scene appears to be on a slow camera orbit or the pedestal itself is rotating.

**t=5.17s** — Same scene, caption advances to `Make people feel`. Pedestal-man pose is identical to t=4.83 (rotation is subtle, may be very slow or the figure is static while only the scene rotates). Film-strip chrome still visible.

**t=5.52s/5.86s** — Same scene. Captions/text continue to layer in. Confirms the scene is held for ~2 full seconds while the line `Make people feel ___` completes.

**t=6.21s** — **HARD CUT** back to face-cam (presenter alone, full-frame 9:16 now — no split, no B-roll above). White wall background, plants, painting, shelf. Caption `what` in karaoke style centered low. Presenter is mid-gesture (hand near mic, mouth open).

**t=6.55s** — Same face-cam continues. Caption advances to `they found`. Presenter has moved slightly (active hand gesture). **HIGH** confidence the face-cam is a recurring "anchor scene" the edit cuts back to.

**t=6.90s** — **HARD CUT** to **pure black screen, single huge white letter `B`** centered, with caption `is` below in smaller sans-serif. The `B` has a subtle **soft white glow / outer-stroke halo** around it — looks like a kinetic-typography "letter spotlight" beat marking a categorical hit. **MEDIUM** confidence on the glow; clearly visible in 1 frame, may be a brief in/out animation.

**t=7.24s** — **HARD CUT** to a desaturated/sepia-toned still of Marlon Brando as Don Corleone (The Godfather, recognizable hand-up gesture, bow-tie, backlit grid blinds behind head — iconic frame). Overlaid: the huge white serif `B` is now centered over Brando's mouth, and a caption `the most` sits just below it. **HIGH** confidence on the use of a recognizable film still as B-roll.

**t=7.59s/7.93s** — Same Brando shot, caption advances to `reliable sound` (per-word karaoke). The big `B` letter remains pinned over Brando's mouth as an anchor — a clever visual where the letter "speaks." Brando appears static (the frame is a still photo, not a clip).

**t=8.28s** — Same Brando still, caption advances to `reliable sound` still visible. The big `B` continues. **HIGH** confidence this Brando-still beat is held for ~2 seconds with successive per-word captions.

**t=8.62s** — Same Brando shot. Caption flips to `in English`. The `B` is pinned in the same screen-position.

**t=8.96s** — **HARD CUT** to a new B-roll: a man (back-of-head, dark jacket, dark pants) **running through a yellow-flowered green field** under cloudy sky, deep-focus trees on the horizon. Caption: `feels alive` with a large white serif `V` overlaid on the runner's torso. **HIGH** confidence on the new shot.

**t=9.31s/9.65s** — Running-man field shot continues; same `V` overlay (`V` for "vivid"/"vibrant"? — the recurring uppercase-serif anchor letter pattern continues). The shot has motion (runner advancing through frame, slight handheld bob) so this is a real video clip, not a still.

**t=10.00s** — Still on the running-man field shot. Caption `V / feels alive` still on screen. End of analyzed window.

---

## Catalog of distinct visual elements & animations (0–10s)

1. **Split-screen B-roll-over-face-cam** (HIGH): top half ~55% B-roll/graphic, bottom half ~45% presenter face-cam. Used at t=0.00–1.72 and again briefly at end of segment 1 (t=3.45 inferred). The seam is a hard horizontal line, not a feathered blend.
2. **Full-frame face-cam anchor** (HIGH): same presenter, same room, used as a recurring "return to speaker" scene at t=6.21–6.55.
3. **Karaoke-style word-by-word captions** (HIGH): single-word or short-phrase white sans-serif (looks like Inter Bold or SF Pro Display Bold), centered, with a subtle drop shadow / dark outline for legibility. New word per ~0.3–0.5s.
4. **Serif drop-cap "topic word"** (HIGH): a large white italic serif (looks like Playfair Display or similar) word pinned to the B-roll above (e.g. `Blackberry` with oversized italic `B`). This is held across multiple karaoke captions — it labels the current concept.
5. **Mega-letter anchor** (HIGH): a single huge sans-serif white letter (e.g. `B`, `V`) centered on screen with a soft glow halo, acting as a typographic "tab marker" tied to the topic word's first letter. Used at t=6.90, 7.24–8.62 (`B`), and t=8.96–10.00 (`V`).
6. **Logo orbit / arc-conveyor** (HIGH — user-flagged pattern, CONFIRMED): white-square app-icon tiles (Pentium, Swiffer, Blackberry) travel along a faint concentric-arc track on a deep blue gradient, entering from one side, passing through center with a slight tilt, and exiting the other. The arcs are visible as thin white concentric guide-lines. The icons appear individually labeled below by name as each reaches center.
7. **Deep blue gradient stage** (HIGH): solid royal-blue gradient (~#1B3A7E core, ~#2E5BB0 highlight upper-left), used as the "logo carousel" backdrop. Includes 2–3 faint white concentric arc lines.
8. **Cream-paper textured background with mustard circle** (HIGH): used in the top-half B-roll at 0:00–1.72. Mustard/olive ochre filled circle (~#A99845) over speckled cream paper — likely a hand-drawn / illustrated B-roll asset, not a photograph.
9. **Vintage film-strip chrome** (HIGH): `UNFOLD 2010TX` sprocket text printed vertically down both left and right edges of frame, with `20`/`21`/`11`/`12` tick numbering and tiny film-perforation marks. Used at t=3.79–6.21 (across two consecutive scenes — Spent/40-years and pedestal-man).
10. **L-bracket corner frame** (HIGH): thin black 90° corner brackets framing a centered grey card, used at t=3.79–4.14. A "viewfinder / camera-frame" chrome element.
11. **Layered typographic stack** (MEDIUM): on the Unfold-grade card, multiple text lines (`Spent`, `40 years`, `To see how`) appear at different opacities and weights, layered overlapping in the same card region rather than reflowing — a deliberate poster-typography effect.
12. **Animated ink-swoosh / brushstroke wipe** (MEDIUM): a black organic brushstroke sweeps diagonally across the grey card at t=4.14 — possibly used as a scene-transition flourish.
13. **3D pedestal/figure render** (HIGH): white paper-cutout man on white plinth, isometric 3D look, against pure black. The scene appears either subtly rotating or held static across t=4.48–5.86.
14. **Iconic film still as B-roll** (HIGH): The Godfather / Marlon Brando still at t=7.24–8.62 with overlaid mega-letter and karaoke caption.
15. **Cinematic B-roll clip** (HIGH): running-man-in-field shot at t=8.96–10.00 — appears to be a stock or licensed cinematic clip used as a visual metaphor.
16. **Lavalier microphone visible** (HIGH): small black lav mic clipped to presenter's sweatshirt — visible in all face-cam frames, signals "real recorded audio."

---

## On circular / rotating arrangements (user-flagged question)

**CONFIRMED — HIGH confidence.** Between approximately **t=1.72 and t=3.45**, the video runs a dedicated "logo orbit" stage on a deep-blue gradient background with **2–3 faint white concentric arcs visible as guide-rails**. App-icon-style rounded-square tiles (Pentium, Swiffer, Blackberry, with at least one more probably entering off-screen) travel along these arcs from screen-right to screen-left (reading direction), tilting slightly as if mounted on a rotating carousel wheel. Each tile pauses briefly at center, gets a name label below it (`Pentium`, `Swiffer`, `Blackberry`), then slides off as the next enters from the right. This is exactly the "logos appearing left-to-right in a circular pattern" the user flagged as missing from the prior analysis. The frames at t=2.41 (`pentium` exiting left while Swiffer arrives) and t=3.45 (Swiffer exiting while Blackberry centers) make the orbit motion unmistakable.

---

## Cut cadence

I counted **at least 6 hard cuts** in the 0–10s window (no dissolves, no fade-outs — all instant cuts):

1. t≈1.72 — split-screen B-roll → blue logo-orbit stage
2. t≈3.79 — blue logo-orbit → Unfold-film grey card (`Spent 40`)
3. t≈4.48 — Unfold grey card → black/pedestal scene
4. t≈6.21 — black/pedestal → full-frame face-cam
5. t≈6.90 — face-cam → black screen with mega-`B`
6. t≈7.24 — black mega-`B` → Marlon Brando still + `B` overlay
7. t≈8.96 — Brando still → running-man-in-field clip

That's **~7 cuts in 10 seconds** (one every ~1.4s on average). Cadence is fast but not frenetic — each scene holds long enough to read 2–4 karaoke words. **HIGH** confidence; no blend/dissolve frames were observed between scenes.

---

## What this template would need to replicate

A Remotion/Hyperframes template aiming to replicate this Stephan beat needs **(1) a scene-stack architecture** where any single video sequences 5–10 visually-distinct "beats" of 1–2s each, glued together by hard cuts at caption-word boundaries; **(2) a karaoke-caption engine** that overlays per-word white sans-serif text (Inter Bold or similar, with soft drop-shadow) at the bottom of the safe area, plus a **secondary serif drop-cap layer** that holds a topic-word across multiple captions; **(3) a "mega-letter anchor"** primitive — a giant white sans-serif single letter centered on the screen with a soft glow, used to mark categorical hits ("B is the most reliable…"); **(4) a logo-orbit/carousel** scene type — deep-blue gradient backdrop, 2–3 faint white concentric arc guide-rails, rounded-square white tiles for app icons that translate-and-tilt along an arc from right to left, with a name label below the centered tile; **(5) a vintage-film-stock chrome** scene wrapper (`UNFOLD 2010TX` sprocket text vertically down both edges, plus L-bracket corner marks framing a centered dark card) for "archival fact" beats; **(6) a 3D figure-on-pedestal** asset (or a stand-in static image) for "human/emotion" metaphor beats; **(7) split-screen face-cam-over-B-roll** layout where the top half can be a still illustration with a serif drop-cap and the bottom half is a 9:16-cropped talking head; and **(8) a B-roll slot** that accepts both static film stills (Brando) and short cinematic clips (running-man) — the same caption + mega-letter overlay treatment applies to both. The whole thing runs at a consistent **~7-cuts-per-10-seconds cadence**, hard cuts only, with the face-cam reappearing every 6–10s to "ground" the viewer.
