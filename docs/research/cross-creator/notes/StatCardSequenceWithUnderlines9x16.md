# StatCardSequenceWithUnderlines9x16 ↔ codingfab (NEW — 2026-06 CodingFab-derived)

**Creator pattern:** *SerifStatCardWithEyebrowAndUnderline* (in sequence) — a near-black
editorial stat card built from a small-caps tracked **eyebrow** ("JUST", "AS LOW AS"), a
huge **serif hero figure** with one accent-colored word, a short **accent underline rule**,
and a one-line serif description. Several such cards are shown in **sequence**, advanced by a
small dot pager.

**Source reel:** `references/creators/codingfab/lz0ddzEUTJQ/frames/`
- `frame-02-t14.16s.jpg` — card 1: tracked small-caps eyebrow `JUST`, huge serif hero
  **"3.2 Quadrillion"** (the `3.2` numeral set apart, the word in a glowing teal accent),
  a short accent **underline** beneath the figure, then the serif sub "Tokens Google
  processed last month". Soft radial glow on near-black.
- `frame-04-t28.11s.jpg` — a related transition card ("We've Made This Mistake Before" with
  `Lines of code (2010s) → Tokens consumed (2026)`) — same dark serif editorial register and
  gold arrow accent, confirming the multi-card serif sequence.

**Signature traits:** near-black field + soft radial glow · small-caps tracked eyebrow ·
huge serif hero figure with ONE accent word/number · short accent underline rule under the
figure · serif one-line description · shown as a sequence of stat cards.

## What matches (our render)
Sampled `output/cross-creator/StatCardSequenceWithUnderlines9x16.mp4` @1.6/4.6s:
- **Card 1 is a near-1:1 reconstruction:** small-caps tracked `JUST` eyebrow → huge serif
  **"3.2 Quadrillion"** (the `3.2` distinct from the word, word in glowing teal/green with a
  soft halo) → short accent **underline** → serif sub. This is the codingfab card rebuilt,
  including the glow.
- **It is a SEQUENCE:** a 3-dot pager sits at the bottom; @1.6s dot 1 is the active gold
  pill (card "3.2 Quadrillion"), @4.6s dot 2 is active showing card 2 — small-caps `AS LOW
  AS` eyebrow → huge serif **"12%"** (with the `%` set smaller) → underline → serif sub "AI
  suggestion acceptance at tokenmaxxing teams". The "...WithUnderlines" + "...Sequence"
  naming is fully delivered: each card carries the underline, and cards advance via the
  pager — matching codingfab's paged stat-card cadence.
- **Field + glow on-grammar:** deep near-black with a soft top radial glow, exactly the
  codingfab editorial backdrop.

## What differs (honest)
- **Palette:** ours runs near-black-navy with the brand accent + a **green/teal** hero word
  glow (close to codingfab's teal here) and a **gold** active pager dot — within brand range,
  though the accent word reads green rather than the brand gold. codingfab's exact teal is
  reel-specific, so this is more a coincidental match than a strict brand swap; honest to
  note the accent hue is green, not gold.
- **Eyebrow/number type pairing** matches the serif intent; minor weight/tracking differences
  versus the source are cosmetic, not structural.

**Score: 9/10 — NEW.** The closest of the four to its source: faithful net-new CodingFab
template reproducing the eyebrow → huge-serif-figure-with-accent-word → underline → serif-sub
stat card, run as a dot-paged sequence (every card underlined). Only honest nit is the
green accent-word hue vs. a strict brand gold.
