# TitledDossierCard9x16 ↔ adamrosler

**Note:** Native template is Bilawal V3 N7 "chapter-break dossier card" (kicker + massive condensed hero + amber primary stat + bullet dossier). Ships **`palette: "true-black"`** by DEFAULT (`#000000` paper / `#FFFFFF` ink / `#D4AF37` brand gold) — the OSINT pure-black-gold register, which is the single closest *default* match to adamrosler's pure-black + gold-mono + heavy-display signature in the whole cluster.

**Creator pattern (adamrosler):** pure-black canvas, gold/orange mono accents (`ERROR 2.69`, `step 2,907,583`, `4 KB`), heavy display headers, mono tracked sub-labels, staged sequential element reveals. Source frames `S5ZFkY756IY/_fresh/frame-005` (title-card entrance), `7RhJawm2nw4/_fresh/frame-005` (gold mono `ERROR 2.69` over hero), `frame-030` (gold `step 2,907,583` + mono axis labels).

## Our 4 frames (t=0.5 / 1.5 / 2.8 / 4.2)
- **Canvas:** pure true-black + subtle gold radial grain vignette → adamrosler's pure black, exactly. ✓✓
- **Gold mono breadcrumb** "AI CODING · 2026" + underline; **gold mono kicker** "DOSSIER" (+0.22em) → adamrosler gold-mono tracked labels. ✓
- **Massive condensed gold hero** "AI CODING" (Oswald 700, all-caps) → adamrosler heavy display header. ✓
- **Big gold stat** "$2-3M" (sans-900 tabular) + **mono tracked muted label** "PER PROJECT, PER YEAR" → direct echo of adamrosler's gold figure + mono sublabel. ✓✓
- **Accent-bullet dossier** (gold • + white sans): "Agents handle the loop / Humans review the diff / 10× shipping velocity". ✓
- **Serif-italic muted conclusion** "The new default." ✓
- **Motion (textbook adamrosler staged reveal):** t=0.5 kicker+breadcrumb only (hero pre-blur); t=1.5 hero blur-in + stat in + bullet 1 solid + bullet 2 ghosting (accelerating cascade); t=4.2 all 3 bullets + conclusion settled. Hero blur-in → stat-delay 0.3s → accelerating bullet cascade → conclusion. ✓✓

## Matches
Pure-black canvas, gold-mono color discipline, condensed+mono typographic hierarchy, staged sequential motion — all squarely on adamrosler register. Brand gold `#D4AF37` is the correct brand substitution for his amber.

## Gaps (inherent, not a safe edit)
- No literal diagram chrome (neon-stroke boxes / connector lines / RO badges) — but this is a *text/stat chapter-break card*, a legitimate adamrosler scene type (his title + stat-ledger frames look just like this). Adding diagram boxes would invent structure, not minimally improve — out of scope.

## Score: 8.5/10 — VALIDATED
Cleanest default match in the cluster: pure-black + gold-mono + condensed-display + staged reveal all land without any change. Left UNTOUCHED.

**Edited files:** none. No shared-molecule changes.
