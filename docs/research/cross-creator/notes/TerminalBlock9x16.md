# TerminalBlock9x16 ↔ adamrosler

**Creator pattern:** *TerminalSkillLibrary* — a centered terminal/skill-list block on a
dark background.

**Reference frames:**
- `references/creators/adamrosler/mZgCDFEBna0/frames/frame-030.jpg` — "SKILL LIBRARY":
  white uppercase bold title; a header bar with a gold folder glyph + monospace path
  `~/.hermes/skills/`; a body panel listing `fix_imports.py … SAVED`, `run_tests.sh … SAVED`
  in **green monospace** filenames with right-aligned muted-gray `SAVED` tags; white
  uppercase caption line below (`AND RELOADS IT.`).
- Neighbor frames in `mZgCDFEBna0` ("NEW SESSION", "PERSISTENT MEMORY") use a literal
  3-dot mac-chrome terminal window with mono content and a typewriter `indexing… ready.`
  beat — exactly the chrome our comp uses.

**Signature traits:** dark BG · window chrome (folder-path bar OR 3-dot mac chrome) ·
monospace body · green = success/saved, blue/orange prompt tokens · line-by-line typewriter
reveal · tracked-uppercase section label.

## What matches (strong)
- **Dark background** with a centered terminal window — the render comes out dark even
  though the schema default is `cream`, because the `TerminalBlock9x16` `defaultProps`
  in Root drive the dark + green chrome.
- 3-dot traffic-light mac chrome + centered mono title (`zsh — claude-code · 80×24`) — the
  exact idiom in adamrosler's NEW SESSION / PERSISTENT MEMORY frames.
- Monospace content, **green** `✓ Ready` success row, blue/orange prompt tokens
  (`$ claude code`) — matches the green-success / colored-prompt grammar.
- **Typewriter line-by-line reveal**: frame@1.8s shows 2 lines, frame@4.4s shows 3 lines
  (`✓ Ready` appears later) — same deliberate "indexing… ready." beat.
- Tracked-uppercase section label (`CLAUDE CODE · SUBAGENTS`) and accent-tinted breadcrumb.

## What differs (minor / acceptable)
- adamrosler's SKILL LIBRARY uses a *folder-path header bar* (`~/.hermes/skills/`) rather
  than mac traffic-lights; ours uses the mac-chrome variant. Both are in his corpus, so this
  is a faithful sibling, not a miss. Our copy is our own (correct per framing).

## Decision
**VALIDATED — no edit.** The composition already captures the terminal/skill-library
pattern faithfully: dark chrome, monospace, green success, colored prompt, typewriter
reveal, tracked label. Editing would be change-for-change's-sake.

**Score: 9/10** — VALIDATED. Closest single-system match in the cluster; only difference is
the header-bar-vs-mac-chrome variant choice, both of which exist in adamrosler's own work.
