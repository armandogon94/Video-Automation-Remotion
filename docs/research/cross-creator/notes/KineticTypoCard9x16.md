# KineticTypoCard9x16 ↔ adamrosler

**Note on assignment:** Native template is *@builtbystephan* "Unfold film-frame card" (NF-2) — a warm-cream film frame with sprocket holes, L-bracket corners, kinetic-typo line reveal, brush-wipe exit. Parked in the adamrosler cluster. adamrosler's signature is neon-stroke cards on pure black; a warm film card is a different visual language. Judgeable axis: register of the *canvas* + chrome discipline + motion timing.

**Creator pattern (adamrosler):** pure-black canvas, neon/mono, gold accents. Source frames `S5ZFkY756IY/_fresh/frame-022,026,030`, `7RhJawm2nw4/_fresh/frame-018,020`.

## Our 4 frames (t=0.5 / 1.5 / 2.5 / 4.0)
- **Canvas:** deep navy-black gradient (brand) — already in adamrosler's dark register by default (schema `palette` defaults to `"dark"`; outer canvas is dark, card is paper). ✓ — no cream-canvas mismatch (unlike sibling IllustratedConcept).
- **Film-frame card:** warm-cream `#E8D9B8`, sprocket-hole strips top+bottom, L-bracket corner markers (4×), soft drop shadow. Faithful Stephan chrome.
- **Mono labels:** tracked-uppercase "UNFOLD 2010TX" top, "REEL · 01/12" bottom-right badge — mono discipline matches adamrosler's mono-everywhere habit (different copy, correct).
- **Kinetic-typo reveal (the signature motion):** t=0.5 line 1 "Spent 40 years" solid + "perfecting" mid-fade ghost; t=1.5 all 3 lines ("…consonant traits") revealed; holds at 2.5/4.0. Per-line scale 0.95→1.0 + opacity stagger lands cleanly. ✓ Motion timing is the star and is correct.

## Matches
- Dark canvas register ✓; mono tracked chrome ✓; staged/sequential entrance motion ✓; high typographic hierarchy discipline ✓.

## Gaps (inherent, not safe to "fix")
- The hero **card is warm-cream paper, not neon-on-black** — adamrosler's actual card language. But this is the documented Stephan signature ("the card IS a paper element sitting on the palette backdrop", lines 170-176). Recoloring the card to neon-black would DESTROY this template's identity, not improve adamrosler fidelity — out of scope and unsafe.
- No neon stroke / glow chips (adamrosler) — again inherent to it being a film-frame template.

## Score: 7/10 — VALIDATED
Faithful, high-quality execution of its own (Stephan) signature with the canvas already in the adamrosler dark register and correct mono chrome + kinetic-typo motion. The warm-card vs neon-card difference is a cross-creator template-language gap that cannot be closed without breaking the template. Left UNTOUCHED.

**Edited files:** none. No shared-molecule changes.
