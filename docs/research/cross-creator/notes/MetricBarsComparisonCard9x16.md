# MetricBarsComparisonCard9x16 ↔ codingfab (NEW — 2026-06 CodingFab-derived)

**Creator pattern:** *SideBySideSpecCardsWithMetricBars* — a "Learning Hub / Insights"
app-card layout that compares two (or more) subjects as stacked dark spec cards, each with a
title, a row of small meta facts, and a column of labeled horizontal **metric bars** that
animate-fill to an `X/5` (or `N`) score, finished with a row of category tag pills.

**Source reel:** `references/creators/codingfab/EePNWETYVYA/frames/`
- `frame-02-t12.30s.jpg` — header "Language Insights" bold white + "Comparing 2 languages
  side-by-side." sub; a horizontal **selector pill row** (`Python` active / `JavaScript`
  active / `TypeScript` / `C#` / `Java`); then the **Python** card: title, meta line
  (`Typing: Dynamic` … `Born: 1991`), seven labeled gradient metric bars (`AI/ML Strength
  5/5`, `Web Strength 4/5`, `Systems 2/5`, `Mobile 2/5`, `Enterprise 4/5`, `Performance
  2/5`, `Learning Curve 1/5`) each with the `X/5` value right-aligned, and a bottom pill row
  (`AI` · `Data Science` · `Backend` · `Research`); a second **JavaScript** card begins
  below — the stacked side-by-side comparison.
- `frame-07` floating "7 strategic metrics" annotation chip confirms the 7-bar metric set.

**Signature traits:** dark spec cards · title + small meta facts (`Typing`, `Born YYYY`) ·
column of labeled horizontal metric bars with right-aligned `X/5` values · gradient bar fill
animating left→right · category tag-pill row per card · two cards stacked for comparison.

## What matches (our render)
Sampled `output/cross-creator/MetricBarsComparisonCard9x16.mp4` @1.6/4.6s:
- **Header copy verbatim-on-pattern:** "Language Insights" bold white + "Comparing 2
  languages side-by-side." muted sub — same Learning-Hub idiom.
- **Two stacked dark spec cards** (`Python`, `JavaScript`), each with a small-caps `DYNAMIC`
  eyebrow, the title, and a right-aligned `Born 1991` / `Born 1995` meta fact — same
  title + meta-fact header as codingfab's cards.
- **Seven labeled horizontal metric bars per card** (`AI/ML Strength`, `Web Strength`,
  `Systems Strength`, `Mobile Strength`, `Enterprise`, `Performance`, `Learning Curve`) with
  the **`X/5` value right-aligned** and bars that **animate-fill** (at 1.6s the Python bars
  are partially filled / JS card still settling; by 4.6s both cards are fully scored
  5/5,4/5,2/5,2/5,4/5,2/5,1/5 and 3/5,5/5,1/5,3/5,3/5,3/5,2/5) — the core metric-bar
  mechanic and the right-aligned scoring match exactly.
- **Category tag-pill row** at the bottom of each card (`AI · Data Science · Backend ·
  Research` / `Web · Frontend · Fullstack · Apps`) — the per-card pill row is present.
- Cards carry a hairline gold border + soft inner fill on near-black, on-brand chrome.

## What differs (honest)
- **Bar color:** our metric bars fill **green** (a semantic strength/positive hue) rather
  than the brand gold or codingfab's blue→purple gradient. This is a legible, defensible
  choice (green = "strength" reads instantly) but it is NOT the navy/gold brand accent — the
  one honest palette-discipline gap versus the rest of the brand set. Worth a follow-up if
  strict one-gold-accent parity is wanted; left as the render shows it.
- **No top selector-pill row** (`Python | JavaScript | TypeScript | C# | Java`): we go
  straight to the two comparison cards. Minor density divergence; the cards themselves carry
  the full pattern.

**Score: 8/10 — NEW.** Faithful net-new CodingFab template: "Language Insights" header,
two stacked dark spec cards with title + meta facts, seven animate-filling labeled metric
bars with right-aligned `X/5` scores, and per-card category pills. Honest gap is the green
bar hue (not the brand gold) plus the dropped top selector row.
