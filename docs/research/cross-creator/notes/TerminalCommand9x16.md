# TerminalCommand9x16 ↔ adamrosler

**Creator pattern:** *terminal/console command line* — a `>`/`$`-prompted monospace command
with a live typing cursor and color-coded tokens, on a dark surface.

**Reference frames (FRESH):**
- `references/creators/adamrosler/7RhJawm2nw4/_fresh/frame-015.jpg` — `> forward pass: making a
  guess` in **blue monospace** (`>` prompt).
- `.../frame-020.jpg` — `> error signal traveling backwards` in **orange monospace**.
- `.../frame-025.jpg` — `> every weight nudged toward right` in **green monospace**
  (sampled ≈ #22AA5C). Same `>`-prompt console line, color-coded by semantics.
- `mZgCDFEBna0` (original frames) — literal 3-dot mac terminal with typewriter `indexing… ready.`
  and colored `$ claude code` prompt tokens.

**Signature traits:** dark surface · monospace · `>`/`$` prompt · color-coded tokens
(blue/orange/green) · **character-by-character typewriter with a block cursor** · gray output
lines · tracked-uppercase section label.

## What matches (strong)
4-frame extract of `output/cross-creator/TerminalCommand9x16.mp4`:
- mac 3-dot chrome window on dark, `zsh — claude-code · 80×24`.
- **Character-by-character typewriter with a visible block cursor**: `$ clau█` →
  `$ claude "review the auth█` → full command + `→ Dispatching subagent: security-reviewer`.
  This is exactly adamrosler's "type each line at ~10fps" beat — the live cursor is a precise
  match to his console-typing idiom.
- **Color-coded tokens**: green `claude` command, white args, **gray** output (`Welcome…`,
  `Type /help for commands`), green `→` dispatch arrow — same semantic color grammar as his
  blue/orange/green `>` lines.
- Tracked-uppercase green section label `CLAUDE CODE · SUBAGENTS`.

## What differs (minor / acceptable)
- Ours uses `$`-prompt + full mac window; some of his console lines are a bare `>` line overlaid
  on a diagram. Both idioms are in his corpus (literal terminal in `mZgCDFEBna0`, overlaid `>`
  lines in `7RhJawm2nw4`), so this is a faithful sibling, not a miss. Our copy is our own (correct).
- Sibling of `TerminalBlock9x16` — same chrome; the distinction (live typing-cursor command flow
  vs block reveal) is intentional and both are on-grammar.

## Decision
**VALIDATED — no edit.** Dark chrome, monospace, prompt+cursor typewriter, semantic token colors,
gray output, tracked label all match. The live block cursor is the standout faithful detail.

**Score: 9/10** — VALIDATED. Among the strongest matches in the cluster.
