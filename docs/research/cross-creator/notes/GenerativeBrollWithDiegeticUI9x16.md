# GenerativeBrollWithDiegeticUI9x16 ↔ sahilbloom

**Pass:** deep cross-creator adversarial QA (sahilbloom-kinetic-broll cluster)
**Date:** 2026-06-04
**Footage comp:** the full-frame AI B-roll plate is a cream placeholder here (no Sora/Veo
clip) — judged on the DIEGETIC-UI CHROME / LAYOUT only, per brief.

## Creator signature (sahilbloom) — faked app UI
- **Faked SaaS app-stack** (`fresh-saaslogos-gmail-cal-slack-zoom.jpg`): real Gmail /
  Calendar / Slack / Zoom logos in a row on white, with a kinetic caption.
- **Faked browser/app window** (`anim-01-dense-001.jpg`, "Advice Nº15"): a windowed card
  with chrome dots and a blue radial glow halo behind it.
- Light field, editorial restraint, single accent, glow-behind-window depth.

## Our version (GenerativeBrollWithDiegeticUI9x16.mp4)
Template origin = **Simon Hoiberg V3 N4** "hovering UI" (AI B-roll under a floating Mac-
window UI mockup), cross-compared to sahilbloom.
- **Cream editorial field** `#F2EAD7` (sampled 242,234,215). ✓
- **Gold single accent** `#C8A15C` (sampled 200,161,92): tracked uppercase kicker
  "GENERATIVE B-ROLL · PATTERN" (underlined) + outlined chip "DIEGETIC UI" top-left. ✓
- **Faked macOS editor window** (dark body `#0E1116`, sampled 14,17,22): traffic-light dots
  (red/yellow/green), titlebar path "~/src/compositions/X.tsx", file breadcrumb
  "src › compositions › X.tsx", syntax-highlighted code line (purple keywords / white
  identifiers). Slight `-3°` rotation + a holographic glow box-shadow halo behind the window.

## Compare (skeptic)
- **Diegetic UI / faked-app-window:** STRONG match — both fake a real software window with
  chrome dots; ours adds realistic editor furniture (path/breadcrumb/syntax). This is exactly
  sahilbloom's faked-app-UI-as-B-roll move. ✓✓
- **Glow-behind-window depth:** match — our holographic box-shadow ring echoes sahilbloom's
  blue-glow halo behind the "Advice Nº15" window. ✓
- **Light field + accent discipline:** match — cream paper, single gold accent, tracked
  kicker + outlined chip = editorial restraint. ✓
- **Motion:** all 4 frames identical — the editor-variant window is intentionally STATIC; the
  N4 motion is supplied by the underlying B-roll plate (absent in the placeholder render) plus
  the fixed rotation/glow. Expected for a footage comp rendered without footage; NOT a chrome
  defect. (The terminal variant would animate a typing cursor, but the editor variant holds.)

## Decision: **VALIDATE** — 8/10
Faithful capture of sahilbloom's faked-software-UI signature: light editorial field, single
gold accent, a clean faked macOS window (traffic lights + path + breadcrumb + syntax) with a
glow halo for depth. No collisions, no overflow, crisp chrome. Differing copy (code editor vs
SaaS logos) is correct per the generic-template brief — the SIGNATURE is "faked app UI
floating as B-roll with a glow," which ours nails. Left untouched (any motion fix belongs to
the footage layer or shared MacWindow, both out of scope).

## Recommended (NOT made — shared / out of scope)
- Optional (shared `MacWindow` / footage layer): for placeholder renders with no B-roll, an
  optional subtle entrance (scale/blur-in) or a slow parallax drift on the window would keep
  the editor variant from reading as a frozen still. Low priority — real usage has a moving
  B-roll plate underneath. Shared molecule — described, not edited.
