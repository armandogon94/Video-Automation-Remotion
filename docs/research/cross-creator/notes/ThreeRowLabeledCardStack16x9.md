# ThreeRowLabeledCardStack16x9 ↔ natebjones

**Creator pattern:** N1 "labeled three-row card stack" (ANALYSIS.md §2 N1; the three-axis evaluation-rubric beat).
**Reference frames (cited):**
- `references/creators/natebjones/n0nC1kmztSk/frames/anim-02-*-t285s.jpg` — TRACE / WORKFLOW / PRODUCT × Tool Calls / Task Outcome / User Value (the exact triplet our demo content mirrors).
- `references/creators/natebjones/NRBQmwlILjk/frames/*-t295s.jpg` — same molecular layout, different triplet.
- NOTE: the dense extraction caught the **talking-head** beat at both t285s and t295s, so no clean graphic frame of the rubric is in the corpus. Judged against the documented N1 pattern + Nate's general card vocabulary (NamedCardEquation, fresh-frame process cards, ANALYSIS.md §159 "role-coded accent borders").

## Signature pattern
Three stacked horizontal cards on dark slate. Each card: left "label tab" (uppercase, tracked, accent-colored) → thin vertical accent rule (ramps in after the card body fades) → right title (white, ~36-48px Inter). Single caption pill below with exactly ONE orange keyword, fades in after row 3 lands. Dark-slate chassis, handle/CTA pill bottom-right.

## What matches (our render)
- Three stacked horizontal label-tab cards — exact N1 structure. Demo content (TRACE/WORKFLOW/PRODUCT × Tool Calls/Task Outcome/User Value) IS the cited reference triplet.
- Left label tab (uppercase, tracked, accent-colored: orange/gold/cyan) + thin **vertical accent rule** + right white title (44px, mid of the 36-48 range). Filled translucent rounded-rectangle card with the rule as the internal divider — matches Nate's "label-left + title-right card rows" (NOT an outline card; the rule is the divider).
- **Motion (frame-by-frame confirmed):** frame-1 (t=0.5s) — only row 1 present and its vertical rule has NOT yet ramped (rule draws at f10-14); frame-2 (t=1.7s) — all three rows placed with all three rules drawn; frame-4 — caption pill faded in last with orange keyword ("evaluate"). Exactly the documented "row fades → rule ramps → next row … caption pill last" cadence.
- Accent discipline (orange/gold/cyan) tracks Nate's 5-color band; handle chip bottom-right.

## What differs (minor / out of scope)
- No left-edge ghosted beanie/glasses watermark; no center-glow radial-gradient backdrop — both live in the shared `DarkSlateChassis16x9` (NOT editable here). See recommendation.
- Handle is plain gold text, not Nate's beanie-icon CTA pill — shared `DarkSlateChassis16x9`.

## Verdict
Textbook-faithful capture of the N1 labeled-three-row-card-stack pattern: layout (label tab → rule → title), the rule-ramp-after-fade motion, per-row accent coding, and the orange-keyword caption pill all match. The card chrome (filled card + vertical rule divider) is the correct reading for THIS pattern (the outline-card treatment belongs to NamedCardEquation, a different Nate pattern). No clear, safe, reference-justified improvement exists. **No edit — VALIDATE.**

**Score: 8.5/10**

## Recommendation (not made — shared molecule)
`DarkSlateChassis16x9` could optionally render (a) the faint center-glow radial gradient and (b) a large left-edge ghosted-glyph watermark, to match Nate's fullscreen-card backdrop across all consuming comps. Deferred — chassis is shared by ~21 compositions.
