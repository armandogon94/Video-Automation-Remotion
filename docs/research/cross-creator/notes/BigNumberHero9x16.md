# BigNumberHero9x16 ↔ motiondarwin

**Creator:** @motiondarwin — Brevo "99% deliverability" client stat card.
**Reference frames:** `references/creators/motiondarwin/DN6w6Tjgbzx/frames/frame-04..06` (the brief pointed at `C_oq4VFvnpC`, but those 8 frames are a 3D spinning-wheel intro — the actual stat-card pattern lives in `DN6w6Tjgbzx`, confirmed by `motiondarwin/ANALYSIS.md` §T3 line 59).

## Signature pattern
- ONE massive numeral ("99%") in heavy weight, dark ink, dominating the card.
- A small contextual label ("deliverability") tucked directly beneath the number, same dark ink, lighter weight, ~40% of the numeral size. Number + label are stacked tight and centred.
- Solid pastel brand-color background (Brevo pastel green).
- Number+label sit inside a rounded white card with decorative 4-point sparkle accents at the corners (Darwin-specific chrome).

## What matches (our 15× / "Gemini 3.2 Flash" render)
- Huge Inter-900 numeral ("15") dominates the vertical frame, dark ink — exactly the "huge number + tiny label" hierarchy ANALYSIS.md §T3 explicitly calls "a close cousin of our existing BigNumberHero9x16".
- Small contextual subtitle ("más barato que GPT-5") tucked directly beneath the number, dark ink — the label-beneath-number stack is faithful.
- Motion beat is right: number springs/counts in first (frame 0 shows numeral already landed, subtitle absent), subtitle fades in ~0.3s later — matches "stat lands, then context appears".
- Editorial restraint, single accent family, generous negative space.

## What differs (correctly, per brief)
- Our brand: cream paper + navy/red accent instead of Brevo pastel green. Correct — these are generic templates on our own brand.
- Our copy is Spanish/English ("15×", "más barato que GPT-5") not "99% deliverability". Correct — differing copy is expected.
- No rounded card / no sparkle accents. This is Darwin-specific *client-illustration* chrome, not the transferable signature. ANALYSIS.md treats the card as incidental; the transferable signature is the number→label hierarchy, which we have. Our family DNA is deliberately card-less editorial.
- We render the "×" suffix in accent at ~70% (a deliberate house suffix-discipline pattern); Darwin keeps "%" in the same ink. Acceptable editorial divergence — it reads as the same "big number with unit" gesture.

## Score: 9/10 — VALIDATED
Faithful capture of the giant-numeral + small-label-beneath hierarchy and the land-then-context motion beat. The only non-transferable element (rounded card + sparkles) is creator-specific illustration chrome, deliberately omitted by our editorial family. No change made.
