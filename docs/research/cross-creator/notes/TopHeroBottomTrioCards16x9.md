# TopHeroBottomTrioCards16x9 ↔ natebjones

**Creator pattern:** TreeOfChildCardsWithEmphasisPill / N4 hero+trio (ANALYSIS-VOTE1 §4 #2 / §4 anim-5; ANALYSIS.md §2 N4).
**Reference frames:**
- `references/creators/natebjones/FtCdYhspm7w/frames/vote1-anim-02-frame-001-t337s.jpg` — canonical TreeOfChildCards: monoline DB icon + "Tool Registry" parent header + connector lines + child cards (Description cyan / Risk Level gold / Capabilities violet / Permissions orange), each a full thin colored outline + monoline icon.
- `vote1-anim-05-frame-001-t1315s.jpg` — parent header + stacked child cards + bottom emphasis pill ("Constrain roles sharply…", orange keyword).
- N4 anchors `ogTLWGBc3cE@920s` / `zP6TnEiueEc@830s` (hero headline card + 3 supporting points; not in this frame dir).

## Signature pattern
A parent/hero header element on top; below it a row of child/supporting cards (3–4), each with a colored accent + uppercase label; a bottom emphasis pill with exactly one orange keyword. Hierarchy carried by VERTICAL POSITION, not type weight. Motion: hero lands first, children reveal left→right, pill fades last.

## What matches (our render)
- Hero card on top (orange top-accent border, "THE SHIFT" label + large white headline) + exactly 3 supporting cards below + caption/emphasis pill — exact N4 structure.
- Supporting cards: colored left-accent borders (orange/gold/cyan), uppercase tracked labels + white headlines.
- Motion: frame-1 (t=0.5s) shows the hero landed but the trio not yet risen; later frames show all three placed and the pill faded in — matches "hero drops, trio rises L→R, pill last."
- Accent discipline (orange/gold/cyan) tracks Nate's palette; one orange keyword ("framing") in the pill.

## What differs (intentional / unavailable-anchor)
- Nate's TreeOfChildCards children use a **full thin outline + monoline icon** (the icon-heavy variant, anim-02/05); ours use a **left-accent border, filled translucent card, no icon** — the icon-light "headline + supporting points" reading the N4 JSDoc explicitly documents ("Stripe Press headline + supporting points").
- Hero uses a **top** accent border; children a **left** accent border. Both valid accent-rule interpretations.
- No parent→child connector lines (Nate's tree variant has them; the N4 hero+trio variant does not).

## Verdict
Faithful capture of the N4 hero+trio pattern — vertical hierarchy, left→right child reveal, emphasis pill with orange keyword, correct accent band. **No edit — VALIDATE.**

**Score: 8/10**

## Recommendation (optional, not made)
If we want to lean toward the icon-rich TreeOfChildCards variant, a future enhancement could add an optional monoline icon slot + full outline per child card. Not a defect for the N4 pattern; deferred.
