# ConcentricHierarchyRadialCallout9x16 ↔ codingfab (NEW — 2026-06 CodingFab-derived)

**Creator pattern:** *NestedConcentricRingsWithRadialCallouts* — a containment
hierarchy drawn as a set of nested concentric rings (outermost = broadest concept,
innermost filled core = the narrowest/punchline concept), each ring labeled, with
leader-line callouts radiating out to icon + title + one-line description chips.

**Source reel:** `references/creators/codingfab/XrGDv4D6_ak/frames/`
- `frame-02-t16.50s.jpg` — "Three Nested Layers" (orange serif title). Four nested
  rings: **AI** (blue outer) ⊃ **ML** (teal) ⊃ **DL** (purple) ⊃ **Gen AI** (filled
  magenta core). Ring labels sit inside each band (`AI`/`ML`/`DL` white serif), and four
  radial leader lines run out to left-margin callout chips — each an **icon tile + bold
  title + muted one-line description** (`AI — the broad field`, `ML — learns from data`,
  `DL — neural networks`, `Gen AI — creates new content`). One accent color per ring.
- `frame-06-t49.10s.jpg` — payoff card: "One Field. Three Layers." white bold sans, gold
  accent underline, "Watch the full breakdown — link" sub. Establishes the title/underline
  idiom this template reuses for its header.

**Signature traits:** nested concentric rings · per-ring band label · filled innermost
core for the climax concept · radial leader lines to icon+title+description callout chips ·
one accent hue per ring · bold title + accent underline header.

## What matches (our render)
Sampled `output/cross-creator/ConcentricHierarchyRadialCallout9x16.mp4` @0.4/1.6/3.0/4.6s:
- **Header is on-grammar:** `AI ⊃ ML ⊃ DEEP LEARNING` mono breadcrumb (the containment
  relation spelled out with the ⊃ glyph), "One field. Three layers." bold sans hero, and a
  short gold accent underline — directly echoes codingfab's "One Field. Three Layers." +
  gold-underline payoff card.
- **Concentric rings render and reveal outward-in:** by 1.6s two ring bands are drawn with
  an animated gold sweep arc on the innermost ring; by 4.6s the full nested stack is settled
  with a filled core.
- **Per-layer labels + descriptions stack on-screen** in reveal order: `Artificial
  Intelligence` / "Any system that mimics human reasoning", `Machine Learning` / "Learns
  patterns from data", `Deep Learning` / "Multi-layer neural networks", `Generative AI` /
  "Creates new text, images, code" — same icon-less label+one-liner callout content as
  codingfab's four chips, and the innermost concept (`Generative AI`) is the gold-accented
  climax, mirroring his filled magenta `Gen AI` core.
- **Radial callout chips** appear around the rings in the settled frame (`The umbrella`,
  `Neural nets — Stacked layers learn features themselves`, `Data-driven — Improves with
  more examples`, `The newest layer — LLMs & diffusion live here`) with leader lines back to
  the rings — the radial-callout half of the named pattern is present.

## What differs (honest)
- **Palette:** brand navy/gold swap of codingfab's per-ring rainbow (blue/teal/purple/
  magenta). Our rings are tonal navy with a single gold accent (sweep arc, climax label,
  underline) rather than one saturated hue per ring. This is the intentional Armando brand
  discipline (one accent), not a structural miss — codingfab's color-coded rings are a
  correct, intentional divergence for a mono-accent brand.
- **Leader-line geometry** reads tighter/denser than codingfab's long full-height leaders to
  the screen margins; ours keeps callouts closer to the rings. Cosmetic, not a pattern gap.

**Score: 8/10 — NEW.** Faithful net-new CodingFab template: nested concentric containment
rings, per-ring labels, filled climax core, radial icon/title/description callouts, and the
title + accent-underline header idiom. Distance to the reference is the deliberate
mono-gold brand swap of his color-coded rings.
